# Zava Learning — secrets for SRE Agent configuration.
# Copy to `.env` and fill in. NEVER commit the filled-in file (it is .gitignored).

# --- GitHub ----------------------------------------------------------------
# owner/repo the agent opens code-fix PRs against (same repo hosting src/ and infra/).
GITHUB_REPO=
